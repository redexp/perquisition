<% layout('layout') %>

<% appendStyles([
    '/js/lib/Croppie/croppie.css'
]) %>

<% appendScripts([
    ['lib/Croppie/croppie.js', 'croppie'],
    'views/form.js',
	'login/registration/views/registration-form.js',
    'student/profile/**/*.js'
]) %>

<%- renderTranslationsFor('student/profile') %>

<%- partial('student/header') %>

<form id="profile-form" class="p form-sm">
    <div class="form-group">
        <label><%= __('auth.email') %></label>
        <div data-email></div>
    </div>
    <div class="form-group">
        <label><%= __('auth.name') %></label>
        <input name="name" type="text" class="form-control" autocomplete="off"/>
        <div data-name_required class="form-error"><%= __('main.required') %></div>
    </div>
    <div class="form-group">
        <label><%= __('auth.photo') %></label>
        <div class="row-top">
            <label class="btn btn-sm btn-primary photo-btn">
                <%= __('auth.upload_photo') %>
                <input data-photo type="file" class="hidden-file-selector"/>
            </label>
            <div data-cropper></div>
        </div>
        <div data-photo_required class="form-error"><%= __('main.required') %></div>
    </div>
    <div class="form-group">
        <label><%= __('auth.password') %></label>
        <input name="password" type="password" class="form-control" autocomplete="off"/>
    </div>
    <div class="form-group">
        <label><%= __('auth.confirm_password') %></label>
        <input name="confirm_password" type="password" class="form-control" autocomplete="off"/>
        <div data-password_invalid class="form-error"><%= __('main.invalid') %></div>
    </div>

    <%- partial('components/submit') %>
</form>